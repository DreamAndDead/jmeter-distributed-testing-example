# jmeter-distributed-testing-example

An example repo for blog post

## setup

setup Target

```
$ cd setup/target
$ sudo docker-compose up -d
```

setup Collector

```
$ cd setup/collector
$ sudo docker-compose up -d
```
